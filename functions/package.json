{
  "name": "counter-news-functions",
  "description": "Cloud Functions do Counter-News, incluindo scraping de partidas ao vivo do HLTV",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "echo \"sem lint configurado\""
  },
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.0",
    "axios": "^1.7.2",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.12.0"
  },
  "private": true
}